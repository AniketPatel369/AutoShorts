# Auto Shorts — v0.1 Dependencies
# Core
yt-dlp>=2024.0.0

# Later phases (uncomment when needed):
# mlx-whisper                 # v0.2 — Transcription
# ollama                      # v0.3 — LLM analysis
# ffmpeg-python               # v0.4 — Video cutting
# requests                    # v0.6 — Web scraping
# beautifulsoup4              # v0.6 — HTML parsing
# googlesearch-python         # v0.6 — Google search
# scenedetect[opencv]         # v0.8 — Scene detection
# sentence-transformers       # v1.0+ — Embeddings
# fastapi                     # v1.0 — Web UI
# uvicorn                     # v1.0 — Web UI server
# jinja2                      # v1.0 — Web UI templates
